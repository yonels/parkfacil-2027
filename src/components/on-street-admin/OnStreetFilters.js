"use client";
export default function OnStreetFilters({filters,setFilters,options={}}){const select=(key,label,rows)=><label className="text-xs font-semibold text-slate-600">{label}<select value={filters[key]||""} onChange={e=>setFilters({...filters,[key]:e.target.value})} className="mt-1 block w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm"><option value="">Todos</option>{(rows||[]).map(x=><option key={x.id} value={x.id}>{x.name||x.code}</option>)}</select></label>;return <div className="grid gap-3 rounded-2xl bg-white p-4 sm:grid-cols-2 xl:grid-cols-5"><label className="text-xs font-semibold text-slate-600">Fecha<input type="date" value={filters.date||""} onChange={e=>setFilters({...filters,date:e.target.value})} className="mt-1 block w-full rounded-xl border border-slate-200 px-3 py-2 text-sm"/></label>{select("parkingId","Estacionamiento",options.parkings)}{select("areaId","Área",options.areas)}{select("streetId","Calle",options.streets)}{select("segmentId","Tramo",options.segments)}</div>}
