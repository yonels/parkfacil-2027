import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
const read=p=>readFile(new URL(p,import.meta.url),"utf8");
test("Etapa 7B relaciona documentos con reserva atómica, snapshot, RLS e inmutabilidad",async()=>{const m=await read("../../../supabase/migrations/20260811180000_billing_credit_debit_notes.sql");assert.match(m,/document_reference_id/);assert.match(m,/billing_document_lines/);assert.match(m,/for update/gi);assert.match(m,/CREDIT_NOTE_EXCEEDS_AVAILABLE/);assert.match(m,/idempotency_key=p_idempotency_key/);assert.match(m,/enable row level security/);assert.match(m,/revoke all[\s\S]*anon,authenticated/);assert.match(m,/BILLING_ISSUED_DOCUMENT_IMMUTABLE/);assert.match(m,/CREDIT_NOTE_ISSUED/);assert.match(m,/DEBIT_NOTE_ISSUED/)});
