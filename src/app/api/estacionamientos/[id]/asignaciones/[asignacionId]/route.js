import { NextResponse } from "next/server";
import { getSupabaseAdminClient } from "@/lib/supabaseServer";
import { operationalError } from "@/lib/parkingApi";
export async function PATCH(request, { params }) { try { const { asignacionId } = await params; const input = await request.json(); const data = { operator_id: input.operatorId, number_from: Number(input.numberFrom), number_to: Number(input.numberTo), max_vehicles: Number(input.maxVehicles), valid_from: input.validFrom, valid_until: input.validUntil || null, start_time: input.startTime, end_time: input.endTime, days_of_week: input.daysOfWeek || [], status: input.status, supervisor_id: input.supervisorId || null, notes: input.notes || "", updated_at: new Date().toISOString() }; const result = await getSupabaseAdminClient().from("operator_assignments").update(data).eq("id", asignacionId).select("*").single(); if (result.error) throw result.error; return NextResponse.json({ data: result.data }); } catch (error) { return operationalError(error, "No fue posible actualizar la asignación."); } }
